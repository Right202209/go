{
    "version": 2,
    "functions": {
      "api/*.go": {
        "runtime": "vercel-go@1.2.0"  // 指定 Go 运行时版本
      }
    },
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" }
    ]
  }
  